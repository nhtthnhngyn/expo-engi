{
  "formatId": "general.plain-document",
  "skeletonVersion": "v1",

  "note": "Intentionally empty. This format has no standard headings by design (see config.json's reusabilityNote) — it exists so any content has somewhere to export, not to impose structure. This file exists mainly for consistency, so nothing that expects every registered format to have a document-skeleton.json needs a special case for this one.",

  "doc": {
    "type": "doc",
    "content": [
      { "type": "paragraph", "attrs": { "fillIn": true }, "content": [] }
    ]
  }
}
